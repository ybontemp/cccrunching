package org.cccrunching.parser;

import static org.junit.Assert.assertEquals;

public class PatternTest {

    /*
    @Test
    public void testSplit(){
        String input = "1.- Premier point \n Bla Bla \n Bla Bli";
        TextExtractor textExtractor = new TextExtractor();
        Map<String,MeetingItem> items = textExtractor.splitDebateItems(input);
        assertEquals(2, items.size());
    }

    @Test
    public void testDiscussionPattern(){
        String s = "M.Gobert   : Est-ce que je peux inviter les conseillers à prendre place ?";
        TextExtractor.Token token = TextExtractor.Token.parse(s);
        assertEquals(TextExtractor.Token.TokenType.DISCUSSION, token.type);
        assertEquals(s, token.text);
    }
*/
}
